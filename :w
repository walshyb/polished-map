#include <iostream>
#include "map/map.h"
#include <emscripten.h>
#include "state.h"
#include "utils/filereader.h"
#include <emscripten/bind.h>

AppState state;

int main(int argc, char **argv) {
  EM_ASM(
    Module['print'] = function(text) {
      console.log(text);
    };
  );

  std::cout << "Initialized" << std::endl;
  // Initialize settings
  // Initialize map
  // Initialize blocks
  std::cout << "Map Width: " << state.getMap().width() << std::endl;

  return 0;
}
